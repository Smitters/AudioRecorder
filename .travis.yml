osx_image: xcode10.2  
language: swift

script:
  - xcodebuild -scheme AudioRecorder-iOS -sdk iphonesimulator -destination "platform=iOS Simulator,name=iPhone 8" build test | xcpretty -c

after_success:
  - bash <(curl -s https://codecov.io/bash)
